diff -Naur OpenBoard-1.5.3/src/podcast/ffmpeg/UBFFmpegVideoEncoder.cpp OpenBoard-1.5.3-fix/src/podcast/ffmpeg/UBFFmpegVideoEncoder.cpp
--- OpenBoard-1.5.3/src/podcast/ffmpeg/UBFFmpegVideoEncoder.cpp	2019-04-10 09:04:25.000000000 +0200
+++ OpenBoard-1.5.3-fix/src/podcast/ffmpeg/UBFFmpegVideoEncoder.cpp	2019-04-10 22:45:57.168460074 +0200
@@ -744,6 +744,7 @@
 {
     AVFrame* frame = mImageQueue.dequeue();
     writeFrame(frame, mVideoPacket, mController->mVideoStream, mController->mOutputFormatContext);
+    av_freep(&frame->data[0]);
     av_frame_free(&frame);
 }
 
@@ -751,6 +752,7 @@
 {
     AVFrame *frame = mAudioQueue.dequeue();
     writeFrame(frame, mAudioPacket, mController->mAudioStream, mController->mOutputFormatContext);
+    av_freep(&frame->data[0]);
     av_frame_free(&frame);
 
 #if LIBAVFORMAT_VERSION_MICRO < 100
